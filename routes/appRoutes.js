const router = require("express").Router();
//requires express  and calling a router function which creates a new router object for us to use
// now we have the router we can add our routes
// instead of app.get and app.post we use router
// router.post;
//make sure it exports the router that we created in here so we can include all the routes
modules.exports = router;
